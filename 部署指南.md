# 聚合工作台 v5 · Cloudflare Worker 全栈部署指南

> 架构：**前端工作台 + 登录鉴权 + D1 云端数据库 + API 集中网关**，全部在一个 Worker 单文件里。

## 当前线上部署状态（2026-09-06 已部署 ✅）

| 项目 | 值 |
|---|---|
| 线上地址 | **https://service-hub.catkinr-93f.workers.dev** |
| Worker 名称 | `service-hub` |
| 账户 | Catkinr@163.com（`93fe0753b1dce058535acb744bb78794`） |
| D1 数据库 | `service-hub-db`（ID `06ecf15c-22ea-4542-9db6-1b20ecc7e491`，绑定变量 `DB`） |
| 已接入服务 | **Wiki**（真实 API：`https://my-wiki.catkinr-93f.workers.dev/api/articles`，网关 `/api/proxy/wiki`） |
| 待接入服务 | 下载站 / 错误报告分析 / 工单系统 / 模组更新平台 / 个人博客（在设置面板填 API 地址即自动入网关） |

Wiki 映射说明（按其 API 文档）：指标卡读顶层 `total`（文章总数）与 `data[0]`（最新文章/日期）；列表读 `data` 数组（标题/分组/标签/发布日期）。首次登录后前端会自动把含 wiki 的完整配置推入 D1，网关即刻可用。

## 架构总览

```
Cloudflare Worker（service-hub）
├── GET  /                        → 工作台前端（单文件 HTML）
├── POST /api/auth/login          → 登录（服务端校验，发 7 天会话）
├── POST /api/auth/logout         → 注销会话
├── GET  /api/state               → 读取配置+待办（D1）
├── PUT  /api/state               → 保存配置+待办（D1，自动同步 services 表）
├── POST /api/proxy-test          → 用表单当前值测试目标 API（不落库）
├── ANY  /api/proxy/<服务>/<路径>  → ★ 集中网关：读 D1 中该服务的地址与 Token，转发并注入认证
└── D1 数据库
    ├── state     工作台配置 + 待办（单 JSON）
    ├── services  各服务 API 连接配置（含 Token，仅服务端持有）
    └── sessions  登录会话（7 天有效）
```

**这个架构带来的改变：**
1. **数据云端化**：配置、待办、服务 API 参数全存 D1，换设备登录即同步
2. **Token 服务端持有**：各服务的 API Token 只存 D1，浏览器/前端代码完全不接触，泄露面最小
3. **CORS 根治**：前端只调自己的 `/api/proxy/*`，由 Worker 转发到真实服务，不再需要各服务开跨域
4. **登录升级**：账号密码由服务端校验（密码哈希编译在 Worker 内，不随页面公开）

## 部署步骤

### 方式一：Dashboard 网页操作（无需安装任何工具）

1. 登录 [dash.cloudflare.com](https://dash.cloudflare.com) → **Workers & Pages → Create application → Worker** → 命名（如 `service-hub`）→ **Deploy**
2. 先创建 D1：左侧菜单 **Storage & Databases → D1 SQL Database → Create**，名字 `service-hub-db`
3. 回到 Worker → **Settings → Bindings/变量和绑定 → Add → D1 Database**
   - Variable name 填 **`DB`**（必须，代码里按这个名字取）
   - D1 database 选 `service-hub-db` → 保存
4. **Edit code** → 全选删除 → 打开本目录 `worker.js` **全选复制粘贴** → **Deploy**
5. 访问 `https://service-hub.<你的子域>.workers.dev`，出现登录页即成功（登录页会显示「云端后端模式」）
6. 登录后进设置面板填好各服务 API 地址 → 保存 → 数据自动进 D1，`/api/proxy/...` 网关即刻可用

### 方式二：Wrangler CLI（推荐后续维护）

```bash
npm i -g wrangler && wrangler login
wrangler d1 create service-hub-db
# 把返回的 database_id 填进 wrangler.toml
wrangler deploy        # 首次部署，自动建表
```

## 工作台自身 API（API 模块）

登录后（`Authorization: Bearer <会话token>`）可调用：

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/state` | GET | 读取 `{config, todos}`（未初始化返回 `{empty:true}`） |
| `/api/state` | PUT | 保存 `{config, todos}`，并同步各服务到网关配置表 |
| `/api/proxy/<服务id>` | GET/POST… | 转发到该服务配置的 API（自动注入认证） |
| `/api/proxy/<服务id>/<子路径>?x=1` | GET | 子路径与 query 透传 |
| `/api/proxy-test` | POST | `{service:{baseUrl,authType,token,headerName}}` 直接试连 |

示例（部署后在自己机器上）：

```bash
TOKEN=$(curl -s https://service-hub.xxx.workers.dev/api/auth/login \
  -H 'content-type: application/json' \
  -d '{"user":"admin","pass":"你的密码"}' | python3 -c 'import sys,json;print(json.load(sys.stdin)["token"])')

# 集中调用下载站 API（Token 由网关注入）
curl -s https://service-hub.xxx.workers.dev/api/proxy/downloads -H "Authorization: Bearer $TOKEN"

# 带子路径
curl -s https://service-hub.xxx.workers.dev/api/proxy/downloads/file/42 -H "Authorization: Bearer $TOKEN"
```

## 登录

- 账号 `admin`，密码同 v3（部署者保管）
- 修改密码：改 `聚合工作台.html` 中 `WB_ENV.LOGIN`（控制台 `passHash("新密码")` 生成哈希）→ 重新构建 worker.js → 重新部署
- 会话 7 天有效，过期自动要求重新登录

## 安全说明

- 登录校验、Token 注入都在服务端发生；`PASS_SHA256` 编译在 Worker 内（外部不可见），比 GitHub Pages 公开仓库更安全
- 未登录无法访问任何 `/api/*`（除 health/login）；所有数据接口返回 401
- D1 数据库只有你的 Worker 能访问
- 前端页面在未登录时不加载任何业务数据

## 保留的能力

- 双模式自适应：同一份页面，部署在 CF Worker 是「云端后端模式」，本地双击打开 / GitHub Pages 上是「本地模式」（localStorage + 前端哈希登录），互不影响
- 移动端适配、暗色模式、导出 JSON 备份、演示数据等 v2/v3 特性全部保留
